import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // basePath temporaire pendant attente nakora.app
  // Sera changé en "" quand nakora.app sera actif
  basePath: "/nakora",
};

export default nextConfig;
